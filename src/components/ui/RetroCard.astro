---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---
<div class={`bg-brand-bgCard pixel-border retro-shadow hover:bg-brand-bgHover hover:-translate-x-0.5 hover:-translate-y-0.5 transition-all duration-200 ${className}`}>
  <slot />
</div>
